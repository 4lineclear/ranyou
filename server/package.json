{
  "name": "ranyou-server",
  "scripts": {
    "dev": "bun --env-file=process.env run --hot src/index.ts"
  },
  "dependencies": {
    "@googleapis/youtube": "^20.0.0",
    "hono": "^4.6.9",
    "hono-compress": "^0.6.0",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
