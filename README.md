# ranyou

Ran(dom) You(tube)

## Architecture

There are two main packages; Client & Server.

### Server

The server is built on CloudFlare Workers, using TypeScript, and uses it's Turso integration for it's database.

### Client

The client is built on Vanilla TS and uses Vite.
